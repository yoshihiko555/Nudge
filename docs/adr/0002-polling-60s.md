# ADR-0002: 60秒ポーリングを採用する

## 決定
60秒間隔のポーリング + 手動リフレッシュを採用する。

## 理由
- Webhook はサーバが必要でローカル完結要件に反する
- MVP で実装が簡素
- 更新遅延を許容できる（60秒以内）

## 代替案
- Webhook + ローカル中継サーバ
- 変更検知のための短間隔ポーリング（コスト増）

## 影響
- レート制限対応が必須
- バックオフと手動リフレッシュの導線が重要
